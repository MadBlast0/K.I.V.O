{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "The Island overlay: it receives the runtime link and fits its own window to the Island. No other window controls, no requests (SECURITY §9).",
  "windows": [
    "overlay"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "allow-ui-ready",
    "allow-overlay-fit"
  ]
}
