{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "The Island overlay: it receives the runtime link, fits its own window, takes clicks while it has buttons and focus while typing, and sends only its own buttons' requests (island_request allows cancel, say, talk, answer and stop everything). No general runtime requests (SECURITY §9).",
  "windows": [
    "overlay"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "allow-ui-ready",
    "allow-overlay-fit",
    "allow-overlay-interactive",
    "allow-overlay-typing-done",
    "allow-island-request"
  ]
}
